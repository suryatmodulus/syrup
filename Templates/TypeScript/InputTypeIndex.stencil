{% if inputNames.count > 0 %}{% for inputName in inputNames %}export { {{ inputName }} } from "./{{ inputName }}"
export type { {{ inputName }}_Arguments } from "./{{ inputName }}"
{% endfor %}
{% else %}export {}
{% endif %}
