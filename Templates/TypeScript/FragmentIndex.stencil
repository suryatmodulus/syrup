{% if fragmentNames.count > 0 %}{% for fragmentName in fragmentNames %}export type { {{ fragmentName }} } from "./{{ fragmentName }}"
export { {{ fragmentName|lowercasedFirstLetter }}Selections } from "./{{ fragmentName }}"
{% endfor %}
{% else %}export {}
{% endif %}
