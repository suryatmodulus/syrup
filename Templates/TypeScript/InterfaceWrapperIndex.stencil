{% if fragmentNames.count > 0 %}{% for fragmentName in fragmentNames %}export { {{ fragmentName }} } from "./{{ fragmentName }}"
export type { {{ fragmentName }}_Arguments } from "./{{ fragmentName }}"
{% endfor %}
{% else %}export {}
{% endif %}
